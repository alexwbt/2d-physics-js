// import Entity from "entity";
// import { IShape } from "shape";

// export type CollisionFunction = (e1: Entity, e2: Entity) => boolean;
// export type CheckShapeFunction = (s1: IShape, s2: IShape) => boolean;
